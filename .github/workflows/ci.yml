name: CI Pipeline

on:
  push:
    branches: [main]

  pull_request:
    branches: [main]

jobs:
  test-build-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm run install:all

      - name: Build backend
        run: npm run build:backend

      - name: Build frontend
        run: npm run build:frontend

      - name: Test backend
        run: npm run test:backend

      - name: Test frontend
        run: npm run test:frontend

      - name: Lint backend
        run: npm run lint:backend

      - name: Lint frontend
        run: npm run lint:frontend

      - name: Audit dependencies
        run: npm audit --audit-level=high

      - name: Build backend Docker image
        run: docker build -t task-manager-backend -f backend/Dockerfile .

      - name: Build frontend Docker image
        run: docker build -t task-manager-frontend -f frontend/Dockerfile --build-arg VITE_API_URL=http://localhost:5000/api .